# Base64 encoded data
[preset]
name = "base64"
description = "Decode Base64 encoded data to hex"

[records]
format = "lines"

[[detect]]
type = "regex"
pattern = "^[A-Za-z0-9+/]+=*$"

[[detect]]
type = "min_length"
value = 4

[gloss]
transform = "base64"
